version: '3.8'

services:
  the_crew_employee_bot:
    build: .
    command: python -m BOT
    restart: always
    environment:
      - DB_HOST=localhost
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=110412003Aa
