version: '3.12'

services:
#  postgres:
#    image: postgres:16
#    volumes:
#      - pgdata:/var/lib/postgresql/data
#    restart: on-failure
#    ports:
#      - "5432:5432"

  employee_bot:
    build: .
    command:
      - sh -c "python -m BOT"
    restart: always
#    depends_on:
#      - postgres


#volumes:
#  pgdata:

